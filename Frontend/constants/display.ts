export const RESET_VALUE_DROPDOWN = { label: "Clear selection", value: null };

export interface PieChartDataItem {
  name: string;
  population: number,
  color: string;
  legendFontColor: string;
  legendFontSize: number;
  legendFontFamily: string;
}
